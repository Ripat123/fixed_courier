<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CourierMerchant" parent="Theme.Material3.DynamicColors.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CourierMerchant.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
    </style>

    <style name="Theme.CourierMerchant.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CourierMerchant.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

    <style name="Theme.CourierMerchant.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="search_back" parent="ThemeOverlay.AppCompat">
        <item name="android:background">#EFEFEF</item>
    </style>

    <style name="card_main" parent="CardView">
        <item name="android:background">@color/white</item>
    </style>

    <style name="Theme.CourierMerchant.NoActionBar.Fullscreen" parent="Theme.CourierMerchant.NoActionBar">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.CourierMerchant.NoActionBar.ActionBar.Fullscreen
        </item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.CourierMerchant.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/white</item>
        <item name="fullscreenTextColor">@color/white</item>
    </style>
</resources>